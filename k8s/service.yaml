apiVersion: v1
kind: Service
metadata:
  name: health-card-service
  labels:
    app: health-card-app
spec:
  type: NodePort
  selector:
    app: health-card-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
